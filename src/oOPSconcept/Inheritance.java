package oOPSconcept;

public class Inheritance {

	// Inheritance: building relation of sub/child class-super/parent class between classes, sub/child class can inherit property from parent/super class
				// "extends" keyword, defined in child class
	
	// Types of Inheritance:
		// 1. Single Inheritance: child <- parent
		// 2. Mutli-layered Inheritance: grandChild <- child <- parent <- grandParent
		// 3. Hierarichal Inheritance: child1 <- parent -> child2
		// 4. Multiple Inheritance: parent1 -> child <- parent2, not allowed between classes | allowed between class & interface
}
